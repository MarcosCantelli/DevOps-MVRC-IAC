---
- name: Configurar VM de aplicação
  hosts: app_servers
  become: true
  vars:
    java_version: "17"
    node_version: "20"
    app_user: "mvrc"
    app_dir: "/opt/app"
    smb_mount_point: "/mnt/truenas"

  pre_tasks:
    - name: Aguardar SSH ficar disponível
      wait_for_connection:
        delay: 10
        timeout: 120

    - name: Atualizar cache de pacotes
      apt:
        update_cache: yes
        cache_valid_time: 3600

  roles:
    - base
    - java
    - nodejs
    - nginx
    - app-user